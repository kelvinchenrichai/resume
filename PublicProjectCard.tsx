import { ArrowUpRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import { useLocale } from '../app/LocaleContext'; import { ProjectItem } from '../types';
export function PublicProjectCard({project}:{project:ProjectItem}){const {projectText,t}=useLocale();const copy=projectText(project);return <article className="project-card"><Link to={`/projects/${project.slug||project.id}`} className="card-image"><img src={project.coverImage} alt=""/><span>{copy.category}</span></Link><div className="card-body"><div className="eyebrow">{project.year} · {copy.status}</div><h3><Link to={`/projects/${project.slug||project.id}`}>{copy.title}</Link></h3><p>{copy.shortDescription}</p><div className="tag-row">{project.tags.slice(0,4).map(tag=><span key={tag}>{tag}</span>)}</div><Link className="text-link" to={`/projects/${project.slug||project.id}`}>{t('viewCase')} <ArrowUpRight size={15}/></Link></div></article>}
